name: CD

on:
  push:
    branches: [develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

env:
  AWS_REGION: ap-southeast-1

jobs:
  setup:
    name: Setup
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.setup.outputs.environment }}
    steps:
      - id: setup
        run: |
          if [[ "$GITHUB_REF_NAME" == "develop" ]]; then
            echo "environment=staging" >> "$GITHUB_OUTPUT"
          elif [[ "$GITHUB_REF_NAME" == "master" ]]; then
            echo "environment=production" >> "$GITHUB_OUTPUT"
          else
            echo "Unsupported branch: $GITHUB_REF_NAME"
            exit 1
          fi

  build:
    needs: [setup]
    runs-on: ubuntu-latest
    environment:
      name: ${{ needs.setup.outputs.environment }}
    steps:
      - name: Checkout
        uses: hyoaru/actions/checkout@v1

      - name: Setup Node
        uses: hyoaru/actions/setup-node@v1
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Configure AWS credentials
        uses: hyoaru/actions/configure-aws-credentials@v1
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Get configuration
        run: |
          USER_POOL_CLIENT_ID=$(aws ssm get-parameter \
            --name "/itala/user-pool-client-id" \
            --query "Parameter.Value" \
            --output text)
          API_BASE_URL=$(aws ssm get-parameter \
            --name "/itala/api-base-url" \
            --query "Parameter.Value" \
            --output text)

          echo "VITE_AWS_REGION=${{ env.AWS_REGION }}" >> "$GITHUB_ENV"
          echo "VITE_AWS_USER_POOL_CLIENT_ID=$USER_POOL_CLIENT_ID" >> "$GITHUB_ENV"
          echo "VITE_API_BASE_URL=$API_BASE_URL" >> "$GITHUB_ENV"

      - name: Build
        run: npm run build

      - name: Upload build artifact
        uses: hyoaru/actions/upload-artifact@v1
        with:
          name: build-artifact
          path: dist/
          if-no-files-found: error

  deploy:
    needs: [setup, build]
    runs-on: ubuntu-latest
    environment:
      name: ${{ needs.setup.outputs.environment }}
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build-artifact
          path: dist/

      - name: Configure AWS credentials
        uses: hyoaru/actions/configure-aws-credentials@v1
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Get configuration
        run: |
          CLIENT_ARTIFACT_S3_URI=$(aws ssm get-parameter \
            --name "/itala/client-artifact-s3-uri" \
            --query "Parameter.Value" \
            --output text)
          CLOUDFRONT_DISTRIBUTION_ID=$(aws ssm get-parameter \
            --name "/itala/cloudfront-distribution-id" \
            --query "Parameter.Value" \
            --output text)

          echo "CLIENT_ARTIFACT_S3_URI=$CLIENT_ARTIFACT_S3_URI" >> "$GITHUB_ENV"
          echo "CLOUDFRONT_DISTRIBUTION_ID=$CLOUDFRONT_DISTRIBUTION_ID" >> "$GITHUB_ENV"

      - name: Deploy to S3
        run: aws s3 sync dist/ "${{ env.CLIENT_ARTIFACT_S3_URI }}" --delete

      - name: Invalidate CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "${{ env.CLOUDFRONT_DISTRIBUTION_ID }}" \
            --paths "/*"
